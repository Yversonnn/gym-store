import React from 'react';

const Footer = () => (
  <footer className="bg-light text-center py-3 mt-4">
    <small>© {new Date().getFullYear()} PAMPALAKAS — React 19 SPA</small>
  </footer>
);

export default Footer;
